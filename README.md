React modal window that connects to Xelon API and sends e-mail to user for test period

Installation

<pre>git clone https://github.com/podlesny/xelon-form.git</pre>

cd into root directory

<pre>npm install</pre>
